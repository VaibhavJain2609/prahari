{{- $svcNames := dict "registry" "registry" "matchEngine" "match-engine" "correlation" "correlation" "bff" "bff" "web" "web" -}}
{{- range $key, $cfg := .Values.services }}
{{- $name := index $svcNames $key }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prahari-{{ $name }}
  labels:
    {{- include "prahari.labels" $ | nindent 4 }}
    app.kubernetes.io/name: {{ $name }}
spec:
  replicas: {{ $cfg.replicas }}
  selector:
    matchLabels:
      {{- include "prahari.selectorLabels" $ | nindent 6 }}
      app.kubernetes.io/name: {{ $name }}
  template:
    metadata:
      labels:
        {{- include "prahari.labels" $ | nindent 8 }}
        app.kubernetes.io/name: {{ $name }}
    spec:
      containers:
        - name: {{ $name }}
          image: {{ include "prahari.image" (dict "registry" $.Values.global.imageRegistry "name" $name "tag" $.Values.global.imageTag) }}
          imagePullPolicy: {{ $.Values.global.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ $cfg.port }}
            {{- if $cfg.grpcPort }}
            - name: grpc
              containerPort: {{ $cfg.grpcPort }}
            {{- end }}
          env:
            {{- include "prahari.commonEnv" $ | nindent 12 }}
          resources:
            {{- toYaml $cfg.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: {{ $cfg.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: {{ $cfg.port }}
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: prahari-{{ $name }}
  labels:
    {{- include "prahari.labels" $ | nindent 4 }}
    app.kubernetes.io/name: {{ $name }}
spec:
  selector:
    {{- include "prahari.selectorLabels" $ | nindent 4 }}
    app.kubernetes.io/name: {{ $name }}
  ports:
    - name: http
      port: {{ $cfg.port }}
      targetPort: {{ $cfg.port }}
    {{- if $cfg.grpcPort }}
    - name: grpc
      port: {{ $cfg.grpcPort }}
      targetPort: {{ $cfg.grpcPort }}
    {{- end }}
{{- end }}
